# MegaRhino Weekly Sales Report — repo notes

This repo is cloned by a **Claude Code Routine** every Monday ~10 PM Philippine time to build and **enqueue** the weekly Amazon sales report **for each brand opted in via the "Brand Info" sheet**. The full task lives in `ROUTINE_PROMPT.md` (paste it as the routine's Instructions). Setup and troubleshooting live in `SETUP_GUIDE.md`.

## Delivery model (outbox → queue mailer)
The routine does **not** send email and does **not** POST to a webhook. For each brand it builds two self-contained HTML files — a short cover note (the email body) and the full report — and **enqueues** one Missive DRAFT by writing a job JSON into a Google Drive **outbox** folder. The cover note travels as `htmlBodyGz`; the full report travels gzipped inside the job's `attachments` marker (`PDF_FROM_HTML_GZ:<gz>::<name>.pdf`). A separate Google Apps Script "queue mailer" (`Code.gs`, deployed by MegaRhino) polls the outbox on a ~10-minute trigger, sends the cover note as the body, decompresses the report, and renders the PDF using **Google's built-in HTML-to-PDF converter**. A Chrome-based renderer was built to replace that converter but was never set up (see "What changed on 2026-09-07" below), so the built-in converter is what actually runs. The mailer then creates the Missive draft and archives the job. Enqueuing uses the **Google Drive connector**, which is exempt from the sandbox egress allowlist — so delivery no longer depends on network egress, a webhook, a verified sending domain, or a browser fallback. Always a DRAFT; never auto-send. Brands are processed independently — one failure does not stop the rest.

**Config (see `ROUTINE_PROMPT.md`):**
- `OUTBOX_FOLDER_ID = 1Va4VHJFydqAjq9piydQFdeVnPElszFDD` — the folder the queue mailer polls (shared by every project that uses the queue mailer; it processes any job type). Job files are named `job_weekly_<CODE>_<YYYY-MM-DD>.json`.
- `LEDGER_FOLDER_ID = 1cR4lDuXpctVaA9q5Qs4VSrw9vsK95A9x` — holds the Weekly Sales dedup ledger `weekly_sales_sent.json` (key `<CODE>__<weekStart>`). It is a **different file** from the mailer's other `sent_reports.json` ledger, and it must **never** live in the outbox — the mailer treats every `.json` in the outbox as a job to send.
- Job send fields: `sendApp = Missive`, `sendType = Draft`, `sendAs = support@megarhino.com`.
- `missiveLabels` — job field carrying the Weekly Sales Report Missive shared label (`7631fba9-76e7-4d7f-abf2-5f7d0d79d403`), passed to Missive as `add_shared_labels`. It must be set per job, never as a default on the mailer itself — the outbox and queue are shared by many projects, and a mailer-level default would stamp this label on all of their drafts too.

## What changed on 2026-09-07
The Monday report was simplified: the top-12 revenue bar chart and the inventory legend were removed. `_legend_row` and `_revenue_chart` are still defined in `build_report.py` below but nothing calls them anymore. The report now runs header → KPI cards → disclaimer → "Performance by Product" table → footer. One consequence: the green/red pills in the "Available" column no longer have a legend explaining what they mean.

At the same time, the nicer-looking version of the report (its own `<style>` block, flexbox, an SVG donut chart) was moved out of this folder entirely, into a `pending-upgrades/` folder kept beside this one in the maintainer's local project folder — not in this repo — together with the Chrome-based PDF service it needs and its logo file. That service was written but never set up, so it was never actually the thing rendering the Monday report. The README in that folder has the full story and what it would take to turn it on.

**`weekly-sales-report/build_report.py` in this folder is now the file the Monday routine actually runs, and it matches the `MRMSupport/Weekly-Sales-Report` GitHub repo.**

## Files
- `build_report.py` — branded **HTML** report generator (stdlib only; no reportlab/ghostscript/qpdf). `python3 build_report.py <data.json> <report.html> [<cover.html>]` — writes the full report and, if a third path is given, a short cover note. **Both outputs use the same table-based technique**, because the converter that actually renders the PDF (Google's built-in one, see above) and Missive's email renderer share the same weaknesses: both **ignore CSS `background-color` on divs, mishandle `inline-block`, and drop SVG** — but both honor `bgcolor` on table cells and `border`. So every colored fill (KPI cards, header bar, the green/red availability pills, zebra rows) uses `bgcolor`, and layout is `<table>`. Functions: `_doc`, `_header_rows`, `_kpi_cards`, `_disclaimer_row`, `_product_table`, `_footer_row`, `render_report`, `render_cover`. (`_legend_row` and `_revenue_chart` are also defined but no longer called — see "What changed on 2026-09-07" below.) The logo is inlined from `logo_0.png.b64` as a data URI; there is no separate URL-based logo option in this file. Keep the cover light — a summary that points to the attached PDF.
- `logo_0.png.b64` — small red rhino icon, base64 TEXT, inlined into both the report header and the cover note — the only logo asset `build_report.py` uses now that `logo_full.png.b64` has moved out of this folder. **Do not commit raw `.png` binaries.** A prior version shipped a binary PNG and git's line-ending normalization silently corrupted it in the cloud clone. Storing logos as base64 text sidesteps that class of bug.
- `.gitattributes` — defense-in-depth for any future binary asset. If you add it on GitHub, use the web "Add file → Create new file" button and type the filename directly (avoids OS file pickers that hide dotfiles).
- `sample_data.json` — a synthetic multi-line brand payload, only for local testing of `build_report.py`. Not used by the routine.
- `ROUTINE_PROMPT.md` — the self-contained multi-brand weekly workflow.
- The **queue mailer** — deliberately NOT a file in this repo. It is a separate Google Apps Script project MegaRhino owns, shared infrastructure used by many projects (queue sheet `1NhR5UxbQ090WZGeHkesv3CLypWXKGqkUNUfqKj8B--Y`, tab `Queue`; see `SETUP_GUIDE.md`). Keeping no copy here is intentional — an out-of-date mirror is worse than none. What matters to this routine: it reads the Missive token from a Script Property named `MISSIVE_TOKEN`; reads a job's `missiveLabels` field and passes it to Missive as `add_shared_labels`; dedups job files, claims a row before sending it, and quarantines malformed job files instead of failing the whole run; and has a `doPost` web-app entry point. It still renders the PDF with Google's built-in `Blob.getAs('application/pdf')` converter — the Chrome-based renderer that would replace it was written but never set up (parked outside this repo, see "What changed on 2026-09-07" below). Change it in the Apps Script project itself; nothing in this repo affects it.

## Which brands (sheet-driven)
Governed by the **"Brand Info" tab** of Google Sheet `1oZw5mSqO2YDvbPkYAcAz6NaO8PJG4RZcxxrVszyb5pY`: report only on rows where **`Weekly Sales Recipient Trigger` = `Yes`**. Read the tab via the **Google Drive connector `read_file_content`** (Jarvio `get_google_sheet` is not authorized on these tenants). Join sheet rows to `list_brands` by **`Brand Code`** (name fallback); log unmatched `Yes` rows and skip them. The sheet is the **sole authority** on which brands run — no brand is hardcoded in or out. Duplicate entries (same brand_name + marketplace, e.g. GWTD CA) are deduped. Brands with zero sales in the week are still skipped.

## Product lines (per SKU)
**One row per SKU (child ASIN / size-color variation) for every brand.** No parent-ASIN roll-up and no hand-defined family maps — Firehouse included (its old Light/Dark/Tacky SKU map is retired). Each child ASIN with sales is its own line, named by its variation label; drop zero-unit SKUs. `units_sub` reads `across <N> SKUs`.

## Profit method (actual fees)
Profit = Net Sales − Amazon Charges, order-week basis, using ACTUAL fee rates from settled `financialEvents`. Per SKU:
`charges = referral_rate·revenue + fba_per_unit·units + other_per_unit·units`.
- referral_rate ≈ actual Commission / principal (~15%)
- fba_per_unit = actual FBAPerUnitFulfillmentFee / units — blends $3.44 base and ~$8.50 surcharge units (NOT flat $3.44); varies by size, so compute per SKU where the sample allows.
- Ads (Sponsored Products) are EXCLUDED.
- **This profit is only after per-order Amazon fees.** It does NOT subtract account-level fees (advertising, monthly/long-term storage, inbound/removal, refund admin) or product cost (COGS), so actual net profit is lower. Every report carries a `disclaimer` field saying so (rendered as a footnote under the KPI cards by `build_report.py`).

## Data sourcing
Jarvio `sp_api_pull_data` only (never `my_data` — not brand-scoped). Pass each brand's `brand_tenant_id` and use its `marketplace_id`. orderMetrics for client-facing units/revenue (order week); financialEvents for actual fee RATES (settlement-dated) — do not mix the two bases.

## Requirements
Python 3 only — `build_report.py` uses the standard library, so there is nothing to install (`setup.sh` is a no-op kept for compatibility). No reportlab, ghostscript, or qpdf. No network allowlist is required: the only outbound calls are the Google Drive connector (routes through Anthropic, exempt from the egress allowlist), and the actual Missive send happens later inside the Google-hosted queue mailer, not from this routine.
